# Copy this file to ".env" (same folder) and fill in your real key.
# .env is gitignored — it will never be committed.
#
# Get a free key (no credit card, no expiration) at:
#   https://aistudio.google.com -> "Get API key"
#
# Note: on the free tier, Google's terms allow your prompts/responses to be
# used to improve their models. Don't send sensitive data through it.

GEMINI_API_KEY=your-key-here
